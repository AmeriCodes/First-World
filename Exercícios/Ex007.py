# Desenvolva um programa que leia as duas notas de um aluno, calcule e mostre sua média.

print(' ----- Averaging Calculation -----')

grade1 = float(input('Grade 1: '))
grade2 = float(input('Grade 2: '))

avg = (grade1 + grade2) / 2

print(f"The student average is {avg}.")
